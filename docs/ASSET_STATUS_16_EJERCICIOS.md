# Estado de assets de los 16 ejercicios

La integración de datos y filtros se realiza en rama aislada. Los vídeos humanos ya existentes en Supabase se reutilizan cuando corresponden al mismo origen. Los nuevos `ejercicio.mp4` y `preview.png` del ZIP deben publicarse antes de fusionar a `main`; no se crean rutas ficticias en producción.
